name: My First Workflow
on: push    # trigger
jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
            - name: Welcome
              run: echo "My first Github Actions job!"
            - name: List Files
              run: ls
            - name: Read File
              run: cat README.md
